import { UnitTestDirective } from './unit-test.directive';

describe('UnitTestDirective', () => {
  it('should create an instance', () => {
    const directive = new UnitTestDirective();
    expect(directive).toBeTruthy();
  });
});
